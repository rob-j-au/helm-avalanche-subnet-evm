apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-avalanche-subnet-evm.fullname" . }}-test-connection"
  labels:
    {{- include "helm-avalanche-subnet-evm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-avalanche-subnet-evm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
